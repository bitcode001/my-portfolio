---
import CustomPointer from '../utils/CustomPointer.astro'
import MainHead from './MainHead.astro'
interface Props {
	title: string
	description?: string
	keywords?: string
	author?: string
	children?: any
}
const { title, description, keywords, author } = Astro.props
---

<html lang="en">
	<MainHead
		{title}
		description={description ?? ''}
		keywords={keywords ?? ''}
		author={author ?? ''}
	/>
	<body class="bg-bgCol">
		<slot />
		<CustomPointer />
		<!-- Google Tag Manager (noscript) -->
		<!-- <noscript
			><iframe
				src="https://www.googletagmanager.com/ns.html?id=GTM-MNJJQVV7"
				height="0"
				width="0"
				style="display:none;visibility:hidden"></iframe></noscript
		> -->
		<!-- End Google Tag Manager (noscript) -->
	</body>
</html>
